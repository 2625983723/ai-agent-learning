# ============================================================
# AI Agent 学习工程 — 依赖清单
# 安装命令: pip install -r requirements.txt
# 使用清华镜像加速:
#   pip install -i https://pypi.tuna.tsinghua.edu.cn/simple -r requirements.txt
# ============================================================

# --- LangChain 核心 (v1.x) ---
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0

# --- 模型接入 ---
langchain-ollama>=0.2.0        # Ollama 本地模型（免费首选）
langchain-openai>=0.2.0        # OpenAI 兼容 API（可选）

# --- LangGraph ---
langgraph>=0.2.0

# --- 向量数据库与 RAG ---
chromadb>=0.5.0                 # 向量存储（轻量、免费）
langchain-chroma>=0.1.0

# --- MCP 协议 ---
fastmcp>=3.0.0                  # MCP Python SDK

# --- Web & 部署 ---
fastapi>=0.115.0                # API 服务框架
uvicorn>=0.32.0                 # ASGI 服务器
pydantic>=2.9.0                 # 数据校验

# --- 工具库 ---
python-dotenv>=1.0.0            # 环境变量管理
httpx>=0.27.0                   # HTTP 客户端（MCP 用）

# --- 开发/测试 ---
pytest>=8.0.0
pytest-asyncio>=0.24.0          # 异步测试支持
